{
  "name": "my-project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "spec": "jest --coverage --detectOpenHandles"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-jwt": "^6.1.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "jest": "^27.0.3"
  }
}